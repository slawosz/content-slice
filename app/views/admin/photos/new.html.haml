%h1 Photos controller, new action

%p 
  Edit this file in 
  %tt app/views/photos/new.html.erb

= error_messages_for @photo

= form_for(@photo, :action => url(:photos) ) do
  %p
    = submit "Create"

= link_to 'Back', url(:photos)